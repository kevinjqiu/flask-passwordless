============
Installation
============

At the command line::

    $ easy_install flask-passwordless

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flask-passwordless
    $ pip install flask-passwordless